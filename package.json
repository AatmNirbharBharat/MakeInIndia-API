{
  "name": "make-in-india-gh-api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "rollup -c",
    "run": "node build/main.js",
    "generate_api": "npm run build && npm run run",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "normalizr": "^3.6.0"
  },
  "devDependencies": {
    "@rollup/plugin-json": "^4.1.0",
    "@rollup/plugin-run": "^2.0.2",
    "rollup": "^2.17.1"
  }
}
